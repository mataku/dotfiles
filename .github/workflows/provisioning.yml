name: Provisioning

on:
  workflow_dispatch:
  pull_request:
  push:
    branches:
      - develop

concurrency:
  group: ${{ github.head_ref || 'develop' }}
  cancel-in-progress: true

jobs:
  build:
    runs-on: macOS-15
    
    steps:
    - uses: actions/checkout@v6
    - name: Provisioning
      run: |
        export HOMEBREW_BUNDLE_MAS_SKIP="$(grep mas <Brewfile | grep -v brew | awk -F 'id\: ' '{printf $2" "}')"
        /bin/bash -ex setup.sh
